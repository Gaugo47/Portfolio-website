Déposez ici les deux images des mains utilisées par le footer ASCII (AsciiHandsFinale.tsx) :

- hand-human.png  -> main HUMAINE (bras venant de la gauche, doigts vers la droite)  = main GAUCHE
- hand-robot.png  -> main ROBOT   (bras venant de la droite, doigts vers la gauche)  = main DROITE

Fond blanc ou transparent : le fond quasi-blanc (RGB > 238) est automatiquement retire.
Si un fichier manque, un emoji de secours est rasterise a la place.
